[project]
name = "llm-browser-sdk"
version = "0.1.0"
description = "Python SDK for the LLM Browser runtime"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["."]
